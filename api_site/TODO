TODO:
** 添加非担保支付退款接口及权限
    - 需保证账户余额足够, 可能在client端有一个人工审核过程。
** 添加支付sn相关页面的token
    - 目的是保证该页面是由client请求而非人为猜测构造
    - 校验+超时?，后端存储?，一次性?
* 增加单元测试和集成测试
    - 解除每次改动之后的不安
* 改进日志
** 改进异常处理，邮件通知等
* add query_pay_to_bankcard_notify
* add query_pay_notify
** 为api添加hook功能
    - 在调用api时执行指定的任务
    - 在通知时通知hook
    - 定义client与api对于hook的协议(通过query参数)
    - 实现为一种Client
    - 定义hook的配置(name, params, url, notifies(url, params)等)
** 添加支持绿野活动支付按活动分组统计的hook client.
* 提现余额不足报警


DONE:
