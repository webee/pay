[program:pay_pub_site]
command=/home/lvye_pay/projects/pay2/pub_site/gunicorn.sh prod 0.0.0.0 5002
directory=/home/lvye_pay/projects/pay2/pub_site
autostart=true                ; start at supervisord start (default: true)
autorestart=true              ; whether/when to restart (default: unexpected)
startretries=3                ; max # of serial start failures (default 3)
user=lvye_pay                 ; setuid to this UNIX account to run the program
stopsignal = QUIT
stopasgroup = true
redirect_stderr=true          ; redirect proc stderr to stdout (default false)
stdout_logfile=/home/lvye_pay/projects/pay2/logs/pub_site_stdout.log        ; stdout log path, NONE for none; default AUTO
stdout_logfile_maxbytes=100MB   ; max # logfile bytes b4 rotation (default 50MB)
stdout_logfile_backups=10     ; # of stdout logfile backups (default 10)
stderr_logfile=/home/lvye_pay/projects/pay2/logs/pub_site_stderr.log        ; stderr log path, NONE for none; default AUTO
stderr_logfile_maxbytes=100MB   ; max # logfile bytes b4 rotation (default 50MB)
stderr_logfile_backups=10     ; # of stderr logfile backups (default 10)
environment=HOME="/home/lvye_pay"

;process_name=%(program_name)s ; process_name expr (default %(program_name)s)
;numprocs=1                    ; number of processes copies to start (def 1)
;directory=/tmp                ; directory to cwd to before exec (def no cwd)
;umask=022                     ; umask for process (default None)
;priority=999                  ; the relative start priority (default 999)
;startsecs=1                   ; number of secs prog must stay running (def. 1)
;exitcodes=0,2                 ; 'expected' exit codes for process (default 0,2)
;stopsignal=QUIT               ; signal used to kill process (default TERM)
;stopwaitsecs=10               ; max num secs to wait b4 SIGKILL (default 10)
;stopasgroup=false             ; send stop signal to the UNIX process group (default false)
;killasgroup=false             ; SIGKILL the UNIX process group (def false)
;serverurl=AUTO                ; override serverurl computation (childutils)

